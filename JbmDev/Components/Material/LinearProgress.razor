<md-linear-progress @attributes="@CapturedAttributes">@ChildContent</md-linear-progress>

@code{
	[Parameter(CaptureUnmatchedValues = true)]
	public IDictionary<string, object>? CapturedAttributes { get; set; }
	[Parameter]
	public RenderFragment? ChildContent { get; set; }
}