<md-suggestion-chip @attributes="@CapturedAttributes">@ChildContent</md-suggestion-chip>

@code{
	[Parameter(CaptureUnmatchedValues = true)]
	public IDictionary<string, object>? CapturedAttributes { get; set; }
	[Parameter]
	public RenderFragment? ChildContent { get; set; }
}